% ==========================================
%  首页风格：en（基于 en 模板）
%    - logo 居左，标题 / 作者 / 联系方式居中，上方有装饰线
%    - 可选摘要：#113BC1 无填充圆角框，宽度与装饰线对齐，高度随文字量自适应
%    - 未设置 \reportabstract 时不显示摘要
% ==========================================
\thispagestyle{empty}
\begingroup
\topskip 0pt%                                                 % 消除页面顶部自动间距
\parskip 0pt%                                                 % 各元素间距由下方 \vspace 控制
\vspace*{-6em}%                                              % 整体上移（与原 en 模板一致）
% --- Logo（居左）---
\ifx\@reportlogo\undefined\else
    \IfFileExists{\@reportlogo}{%
        \noindent\includegraphics[height=2cm]{\@reportlogo}\par
    }{}%
\fi
\begin{center}
    % --- 上方装饰线（与摘要框左右对齐）---
    \vspace{-1.5em}
    \textcolor[HTML]{315DEC}{\rule{\textwidth}{1.5pt}}
    \vspace{1em}

    % --- 报告标题（大号加粗）---
    {\huge\bfseries \@reporttitle \par}
    \vspace{1em}

    % --- 作者 ---
    {\large \@reportauthor \par}
    \vspace{0.6em}

    % --- 所属组织 / 大学（可选）---
    \ifx\@reportaffiliation\undefined\else
        {\normalsize \@reportaffiliation \par}
        \vspace{0.6em}
    \fi

    % --- 联系方式 ---
    {\normalsize \texttt{\@reportcontact} \par}
\end{center}

% --- 可选摘要（无填充圆角框，无则不显示）---
\ifx\@reportabstract\undefined\else
    \vspace{2em}%
    \begin{tcolorbox}[
        enhanced,
        breakable,
        colback=white,
        colframe=abstractframe,
        boxrule=1pt,
        arc=8pt,
        left=20pt,
        right=20pt,
        top=20pt,
        bottom=20pt,
    ]
    % 摘要标题（居中，位于框内）
    \begin{center}
        {\large\bfseries\color{abstractframe} Abstract}
    \end{center}
    \vspace{0.5em}
    \@reportabstract
    \end{tcolorbox}%
\fi

\vspace{1.5em}%
\endgroup
